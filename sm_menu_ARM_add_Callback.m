function sm_menu_ARM_add_Callback(varargin)
global h

%% 
if h.menu_ARM_add.Value == 2    % combining data then only 3 sources are allowed
    % set num_sources = 3 and disable edit box
else
    
end
